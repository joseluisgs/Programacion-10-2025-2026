<!-- ============================================================ -->
<!-- DialogoNumero.xaml - Diálogo para seleccionar un número -->
<!-- ============================================================ -->
<!-- Ejemplo de diálogo con valor inicial, mínimo y máximo. -->

<Window x:Class="Dialogos.Views.Dialogs.DialogoNumero"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Seleccionar Número" Height="180" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" Text="🔢 Seleccionar Número" FontSize="16" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <!-- Slider -->
        <Slider x:Name="SliderNumero" Grid.Row="1" 
                Minimum="0" Maximum="100" Value="50"
                TickFrequency="5" IsSnapToTickEnabled="True"
                Margin="0,0,0,10"
                ValueChanged="SliderNumero_ValueChanged"/>

        <!-- Valor actual -->
        <TextBlock Grid.Row="2" x:Name="TxtValor" 
                   Text="Valor: 50" FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Botones -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="✅ Aceptar" Padding="20,8" Margin="5" 
                    Background="#4CAF50" Foreground="White"
                    Click="BtnAceptar_Click"/>
            <Button Content="❌ Cancelar" Padding="20,8" Margin="5"
                    Background="#f44336" Foreground="White"
                    Click="BtnCancelar_Click"/>
        </StackPanel>

    </Grid>
</Window>
