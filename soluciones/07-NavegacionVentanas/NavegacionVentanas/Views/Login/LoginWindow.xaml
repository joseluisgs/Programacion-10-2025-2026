<!-- ============================================================ -->
<!-- LoginWindow.xaml - Ventana de inicio de sesión -->
<!-- ============================================================ -->
<!-- Simula una ventana de login que valida credenciales. -->
<!-- Si es exitoso, devuelve DialogResult = true. -->

<Window x:Class="NavegacionVentanas.Views.Login.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🔐 Inicio de Sesión" Height="250" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" Text="🔐 Inicio de Sesión"
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Usuario -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Usuario:" FontWeight="Bold"/>
            <TextBox x:Name="TxtUsuario" Padding="8" Text="admin"/>
        </StackPanel>

        <!-- Contraseña -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Contraseña:" FontWeight="Bold"/>
            <PasswordBox x:Name="TxtPassword" Padding="8" Password="123456"/>
        </StackPanel>

        <!-- Mensaje de error -->
        <TextBlock x:Name="TxtError" Grid.Row="3" 
                   Foreground="Red" TextWrapping="Wrap"
                   Visibility="Collapsed"/>

        <!-- Botones -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="🚀 Iniciar Sesión" Padding="20,8" Margin="5"
                    Background="#4CAF50" Foreground="White"
                    Click="BtnLogin_Click"/>
            <Button Content="❌ Cancelar" Padding="20,8" Margin="5"
                    Background="#f44336" Foreground="White"
                    Click="BtnCancelar_Click"/>
        </StackPanel>

    </Grid>
</Window>
