<Window x:Class="GestionAcademica.Views.Estudiantes.EstudianteDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detalles del Estudiante"
        Width="800" Height="750"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="pack://application:,,,/Resources/icons/app-icon.png"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="👨‍🎓 Detalles del Estudiante"
                   FontSize="24" FontWeight="Bold"
                   Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                   Margin="0,0,0,20"/>

        <!-- Imagen circular -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Border Width="120" Height="120" CornerRadius="60"
                    Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
                <Border.Clip>
                    <EllipseGeometry Center="60,60" RadiusX="60" RadiusY="60"/>
                </Border.Clip>
                <Grid>
                    <Image Source="{Binding Estudiante.Imagen, Converter={StaticResource ImagePathConverter}}"
                           Stretch="UniformToFill"/>
                    <TextBlock Text="Sin imagen"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                               FontSize="11"
                               Visibility="{Binding Estudiante.Imagen, Converter={StaticResource NullToVisibilityConverter}}"/>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Formulario en 2 columnas (solo lectura) -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- DNI -->
            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,15">
                <TextBlock Text="DNI" FontSize="12" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Estudiante.Dni, Mode=OneWay}" IsReadOnly="True"
                         Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                         Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                         BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                         Padding="10,8" FontSize="14"/>
            </StackPanel>

            <!-- Email -->
            <StackPanel Grid.Row="0" Grid.Column="2" Margin="10,0,0,15">
                <TextBlock Text="Email" FontSize="12" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Estudiante.Email, Mode=OneWay}" IsReadOnly="True"
                         Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                         Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                         BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                         Padding="10,8" FontSize="14"/>
            </StackPanel>

            <!-- Nombre -->
            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,15">
                <TextBlock Text="Nombre" FontSize="12" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Estudiante.Nombre, Mode=OneWay}" IsReadOnly="True"
                         Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                         Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                         BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                         Padding="10,8" FontSize="14"/>
            </StackPanel>

            <!-- Apellidos -->
            <StackPanel Grid.Row="1" Grid.Column="2" Margin="10,0,0,15">
                <TextBlock Text="Apellidos" FontSize="12" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Estudiante.Apellidos, Mode=OneWay}" IsReadOnly="True"
                         Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                         Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                         BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                         Padding="10,8" FontSize="14"/>
            </StackPanel>

            <!-- Fecha Nacimiento -->
            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,15">
                <TextBlock Text="Fecha de Nacimiento" FontSize="12" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Estudiante.FechaNacimiento, StringFormat=dd/MM/yyyy, Mode=OneWay}" IsReadOnly="True"
                         Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                         Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                         BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                         Padding="10,8" FontSize="14"/>
            </StackPanel>

            <!-- Ciclo -->
            <StackPanel Grid.Row="2" Grid.Column="2" Margin="10,0,0,15">
                <TextBlock Text="Ciclo" FontSize="12" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Estudiante.Ciclo, Mode=OneWay}" IsReadOnly="True"
                         Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                         Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                         BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                         Padding="10,8" FontSize="14"/>
            </StackPanel>

            <!-- Curso -->
            <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,10,15">
                <TextBlock Text="Curso" FontSize="12" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Estudiante.Curso, Mode=OneWay}" IsReadOnly="True"
                         Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                         Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                         BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                         Padding="10,8" FontSize="14"/>
            </StackPanel>

            <!-- Calificación -->
            <StackPanel Grid.Row="3" Grid.Column="2" Margin="10,0,0,15">
                <TextBlock Text="Calificación" FontSize="12" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Estudiante.Calificacion, StringFormat=F2, Mode=OneWay}" IsReadOnly="True"
                         Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                         Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                         BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                         Padding="10,8" FontSize="14"/>
            </StackPanel>

            <!-- Mayor de edad -->
            <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,10,15">
                <TextBlock Text="Mayor de edad" FontSize="12" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Estudiante.IsMayorEdad, Converter={StaticResource BoolToSiNoConverter}, Mode=OneWay}" IsReadOnly="True"
                         Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                         Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                         BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                         Padding="10,8" FontSize="14"/>
            </StackPanel>

            <!-- Calificación Cualitativa -->
            <StackPanel Grid.Row="4" Grid.Column="2" Margin="10,0,0,15">
                <TextBlock Text="Resultado" FontSize="12" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Estudiante.CalificacionCualitativa, Mode=OneWay}" IsReadOnly="True"
                         Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                         Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                         BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                         Padding="10,8" FontSize="14"/>
            </StackPanel>
        </Grid>

        <!-- Botón cerrar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cerrar" Click="OnCerrarClick" 
                    Style="{StaticResource SecondaryButton}" Width="120"/>
        </StackPanel>
    </Grid>
</Window>