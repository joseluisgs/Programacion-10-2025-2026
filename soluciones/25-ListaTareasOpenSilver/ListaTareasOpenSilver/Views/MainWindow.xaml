<Window x:Class="ListaTareasOpenSilver.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ListaTareasOpenSilver.ViewModels"
        Title="Lista de Tareas - OpenSilver"
        Width="600" Height="500"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titulo -->
        <TextBlock Grid.Row="0"
                   Text="Lista de Tareas - OpenSilver"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Formulario para añadir tarea -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding NuevaTarea, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     Padding="10,8"/>
            
            <Button Grid.Column="1"
                    Content="Añadir"
                    Command="{Binding AgregarTareaCommand}"
                    Margin="10,0,0,0"
                    Padding="15,8"/>
        </Grid>

        <!-- Lista de tareas -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Tareas}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="#F5F5F5"
                            CornerRadius="5"
                            Padding="10"
                            Margin="0,5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <CheckBox Grid.Column="0"
                                      IsChecked="{Binding Completada}"
                                      VerticalAlignment="Center"
                                      Command="{Binding DataContext.ToggleTareaCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                      CommandParameter="{Binding}"/>

                            <TextBlock Grid.Column="1"
                                       Text="{Binding Titulo}"
                                       VerticalAlignment="Center"
                                       Margin="10,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Completada}" Value="True">
                                                <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                <Setter Property="Opacity" Value="0.5"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <Button Grid.Column="2"
                                    Content="Eliminar"
                                    Command="{Binding DataContext.EliminarTareaCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Padding="5"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Contador -->
        <Border Grid.Row="3"
                Background="#E0E0E0"
                CornerRadius="5"
                Padding="10"
                Margin="0,20,0,0">
            <TextBlock HorizontalAlignment="Center">
                <Run Text="Tareas pendientes: "/>
                <Run Text="{Binding Pendientes}" FontWeight="Bold"/>
            </TextBlock>
        </Border>
    </Grid>
</Window>