<Window x:Class="JuegoMosca.Views.Dialog.AcercaDeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Acerca de" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#f5f5f5">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Imagen -->
        <Image Grid.Column="0" Source="/Resources/icons/app.png" Width="100" Height="100" Margin="0,0,20,0"/>

        <!-- Información -->
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <TextBlock Text="Mosca DAW" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Programación 1º DAW" FontSize="16" Margin="0,5,0,15"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="GitHub: " FontSize="14"/>
                <Hyperlink NavigateUri="https://github.com/joseluisgs" RequestNavigate="Hyperlink_RequestNavigate">
                    <TextBlock Text="joseluisgs" FontSize="14"/>
                </Hyperlink>
                <Image Source="/Resources/icons/github.png" Width="20" Height="20" Margin="5,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
